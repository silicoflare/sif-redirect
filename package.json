{
  "name": "sif-redirect",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "fastify": "^5.3.2"
  },
  "scripts": {
    "dev": "bun index.ts",
    "build": "bun build index.ts --outdir dist --format esm --target bun",
    "start": "bun run ./dist/index.js"
  }
}
